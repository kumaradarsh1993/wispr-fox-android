<?xml version="1.0" encoding="utf-8"?>
<!-- Higher-res square fox logo, inset into the adaptive-icon safe zone so the
     mask doesn't crop it and there's no hard edge. -->
<inset xmlns:android="http://schemas.android.com/apk/res/android"
    android:drawable="@drawable/fox_logo"
    android:inset="16%" />
